// Shared visual tokens for the auth screens (welcome, login, register, setup-profile).
// Extracted here because these specific values must stay identical across screens
// that otherwise have independent style files.
export const AuthTheme = {
  fieldBorderColor: 'rgba(232,230,224,0.25)',
  fieldRadius: 14,
  pillRadius: 26,
  buttonHeight: 52,
  backButtonSize: 36,
} as const
